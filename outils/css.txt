.c4wrap{ position:relative; max-width:980px; margin:0 auto; padding:0 10px 22px; overflow-x:clip; }
/* `hidden` doit gagner. Une classe qui pose display:grid ou display:flex bat
   la regle du navigateur `[hidden]{display:none}` par simple specificite, et
   l'ecran cache reste a l'ecran — c'est exactement ce qui affichait le
   vestibule sous le resultat de la partie. */
.c4wrap [hidden]{ display:none !important; }

/* ---- enseigne ----
   En texte, pas en image : « CONNECT 4 » doit rester net a toutes les tailles
   et il n'existe pas de plaque peinte pour ce jeu. */
.c4-titre{ font-family:'Anton',sans-serif; text-align:center; line-height:1;
  font-size:clamp(30px,9vw,58px); letter-spacing:.06em; margin:4px 0 2px;
  background:linear-gradient(180deg,#FFF0C0,#FFC53D 46%,#C98A12);
  -webkit-background-clip:text; background-clip:text; color:transparent;
  filter:drop-shadow(0 3px 0 rgba(0,0,0,.55)) drop-shadow(0 0 18px rgba(255,197,61,.28)); }
.c4-titre span{ background:linear-gradient(180deg,#FFD9DC,#FF4655 46%,#8C1520);
  -webkit-background-clip:text; background-clip:text; color:transparent; }
.c4-sous{ text-align:center; color:var(--dim); font-size:clamp(11px,2.8vw,13.5px);
  margin:0 0 14px; line-height:1.5; }
.c4-sous b{ color:#FFD97A; }

/* ---- panneaux ---- */
.c4-grid2{ display:grid; grid-template-columns:1fr 1fr; gap:14px; align-items:start; }
@media (max-width:760px){ .c4-grid2{ grid-template-columns:1fr; } }
.c4-panel{ position:relative; border-radius:16px; padding:14px 14px 16px;
  background:linear-gradient(180deg,rgba(18,28,52,.94),rgba(8,12,24,.96));
  border:1px solid rgba(255,197,61,.26);
  box-shadow:inset 0 1px 0 rgba(255,214,130,.16), 0 10px 26px rgba(0,0,0,.5); }
.c4-lb{ font-size:10.5px; letter-spacing:1.5px; text-transform:uppercase;
  color:#C9A24A; margin-bottom:9px; display:flex; align-items:center; gap:8px; }
.c4-lb i{ font-style:normal; color:var(--dim); letter-spacing:.4px; text-transform:none; font-size:11px; }
.c4-note{ text-align:center; font-size:11.5px; color:var(--dim); margin-top:10px; line-height:1.5; }

/* ---- choix de la mise : boutons carres dores, le montant est du TEXTE ---- */
.c4-stakes{ display:flex; gap:7px; justify-content:center; flex-wrap:wrap; }
.c4-stakes button{ position:relative; width:clamp(46px,13vw,62px); aspect-ratio:1;
  padding:0; border:0; cursor:pointer; background-color:transparent;
  background-repeat:no-repeat; background-position:center; background-size:100% 100%;
  display:flex; align-items:center; justify-content:center;
  color:#FFD97A; font-weight:900; font-size:clamp(11px,3.1vw,15px); line-height:1;
  text-shadow:0 1px 3px rgba(0,0,0,.95); transition:transform .1s, filter .12s; }
.c4-stakes button.on{ transform:translateY(-4px) scale(1.09);
  filter:drop-shadow(0 0 9px rgba(242,193,78,.9)); }
.c4-stakes button[disabled]{ opacity:.34; cursor:not-allowed; transform:none; filter:none; }

/* ---- boutons d'action ---- */
.c4-btn{ display:block; width:100%; margin:12px auto 0; padding:12px 14px;
  border:0; border-radius:12px; cursor:pointer; font-family:'Archivo',sans-serif;
  font-weight:800; font-size:14px; letter-spacing:.6px; text-transform:uppercase;
  color:#07101F; background:linear-gradient(180deg,#FFE08A,#FFC53D 55%,#E29C10);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.5), 0 5px 0 #8A5E06, 0 10px 18px rgba(0,0,0,.45);
  transition:transform .08s, box-shadow .08s, filter .12s; }
.c4-btn:active{ transform:translateY(3px);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.4), 0 2px 0 #8A5E06, 0 6px 12px rgba(0,0,0,.45); }
.c4-btn[disabled]{ opacity:.4; cursor:not-allowed; filter:grayscale(.45); }
.c4-btn.ghost{ color:#EAF2FF; background:linear-gradient(180deg,#2B3B60,#16223C);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.14), 0 4px 0 #0A1226, 0 8px 16px rgba(0,0,0,.45); }
.c4-btn.ghost:active{ box-shadow:inset 0 1px 0 rgba(255,255,255,.12), 0 1px 0 #0A1226; }
.c4-btn.danger{ color:#FFF; background:linear-gradient(180deg,#FF6B78,#E0293A 60%,#93101D);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.28), 0 4px 0 #5E0A12, 0 8px 16px rgba(0,0,0,.45); }
.c4-btn.small{ width:auto; padding:8px 14px; font-size:12.5px; margin:0; }
.c4-actions{ display:flex; gap:10px; justify-content:center; flex-wrap:wrap; margin-top:14px; }

/* ---- la liste des parties ouvertes ----
   Elle porte la MISE en grand : c'est la seule chose qu'on veut savoir avant
   de s'asseoir, et c'est ce qui sera debite en cliquant. */
.c4-tables{ max-height:296px; overflow-y:auto; display:flex; flex-direction:column; gap:8px;
  scrollbar-width:thin; }
.c4-table{ display:flex; align-items:center; gap:10px; padding:9px 10px; border-radius:12px;
  background:linear-gradient(180deg,rgba(46,123,255,.13),rgba(10,16,32,.6));
  border:1px solid rgba(46,123,255,.34); }
.c4-table .who{ flex:1 1 auto; min-width:0; }
.c4-table .who b{ display:block; color:#EAF2FF; font-size:13.5px; font-weight:800;
  white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.c4-table .who span{ display:block; color:var(--dim); font-size:11px; margin-top:1px; }
.c4-table .bet{ flex:0 0 auto; text-align:right; }
.c4-table .bet b{ display:block; font-family:'Anton',sans-serif; letter-spacing:.5px;
  font-size:clamp(15px,4vw,20px); color:#FFD97A; line-height:1.05; }
.c4-table .bet span{ font-size:10px; color:#C9A24A; letter-spacing:1.1px; }
.c4-table button{ flex:0 0 auto; }
.c4-table.mine{ background:linear-gradient(180deg,rgba(255,197,61,.14),rgba(10,16,32,.6));
  border-color:rgba(255,197,61,.42); }
.c4-vide{ text-align:center; color:var(--dim); font-size:12.5px; padding:26px 8px; line-height:1.6; }

/* ---- demandes de revanche : nominatives, donc hors de la liste publique ---- */
.c4-invite{ border-color:rgba(22,217,127,.45);
  background:linear-gradient(180deg,rgba(22,217,127,.14),rgba(10,16,32,.6)); }
.c4-invite .bet b{ color:#9BFFCB; }

/* ---- le bandeau des deux joueurs ---- */
.c4-hud{ display:flex; align-items:stretch; gap:8px; justify-content:center;
  max-width:560px; margin:0 auto 10px; }
.c4-joueur{ flex:1 1 0; min-width:0; padding:8px 10px; border-radius:12px;
  background:linear-gradient(180deg,rgba(18,28,52,.9),rgba(8,12,24,.95));
  border:1px solid rgba(255,255,255,.08); transition:border-color .18s, box-shadow .18s; }
.c4-joueur .nm{ display:flex; align-items:center; gap:7px; font-weight:800; font-size:13px;
  color:#EAF2FF; white-space:nowrap; min-width:0; }
/* C'est le NOM qui se rogne, pas la ligne entiere : avec l'ellipse posee sur
   le conteneur, la pastille « YOU » disparaissait avant le nom. */
.c4-joueur .nm em{ font-style:normal; overflow:hidden; text-overflow:ellipsis; min-width:0; }
.c4-joueur .sub{ font-size:10.5px; color:var(--dim); margin-top:2px; letter-spacing:.6px;
  text-transform:uppercase; }
.c4-joueur .pion{ flex:0 0 auto; width:15px; height:15px; border-radius:50%;
  box-shadow:inset 0 -2px 3px rgba(0,0,0,.45), inset 0 2px 2px rgba(255,255,255,.35); }
.c4-joueur.j1 .pion{ background:radial-gradient(70% 70% at 35% 28%,#FF8C95,#FF4655 52%,#8C1520); }
.c4-joueur.j2 .pion{ background:radial-gradient(70% 70% at 35% 28%,#8FC0FF,#2E7BFF 52%,#123C86); }
.c4-joueur.actif{ border-color:rgba(255,197,61,.6); box-shadow:0 0 0 1px rgba(255,197,61,.28), 0 0 18px rgba(255,197,61,.22); }
.c4-joueur.moi .nm::after{ content:"YOU"; margin-left:auto; flex:0 0 auto; font-size:9px; letter-spacing:1.2px;
  color:#07101F; background:#FFC53D; border-radius:5px; padding:1px 5px; font-weight:900; }
.c4-pot{ flex:0 0 auto; display:flex; flex-direction:column; align-items:center; justify-content:center;
  padding:6px 12px; border-radius:12px; min-width:96px;
  background:linear-gradient(180deg,rgba(255,197,61,.16),rgba(10,16,32,.7));
  border:1px solid rgba(255,197,61,.34); }
.c4-pot b{ font-family:'Anton',sans-serif; font-size:clamp(15px,4.4vw,21px); color:#FFD97A; line-height:1.05; }
.c4-pot span{ font-size:9.5px; letter-spacing:1.3px; color:#C9A24A; text-transform:uppercase; }
.c4-pot i{ font-style:normal; font-size:11.5px; font-weight:800; color:var(--dim); margin-top:2px;
  font-variant-numeric:tabular-nums; }
.c4-pot i.urgent{ color:#FF4655; animation:c4pulse .9s ease-in-out infinite; }
@keyframes c4pulse{ 50%{ opacity:.35 } }

/* ---- le plateau ----
   Sept colonnes, six rangees, dessinees : aucune image a charger, et la
   grille reste nette sur un ecran de telephone comme sur un 4K. La rangee 0
   du serveur est celle du BAS ; l'affichage inverse donc l'ordre des rangees,
   et c'est fait une seule fois, a la construction. */
.c4-plateau{ position:relative; width:min(100%,520px); margin:0 auto;
  padding:2.4%; border-radius:18px;
  background:linear-gradient(180deg,#1D2C58,#0E1734 60%,#0A1128);
  border:1px solid rgba(255,197,61,.34);
  box-shadow:inset 0 2px 0 rgba(255,214,130,.2), inset 0 -14px 26px rgba(0,0,0,.5),
             0 16px 34px rgba(0,0,0,.55); }
.c4-cols{ display:grid; grid-template-columns:repeat(7,1fr); gap:1.6%; margin-bottom:1.2%; }
.c4-fleche{ aspect-ratio:2/1; border:0; padding:0; background:none; cursor:pointer;
  display:flex; align-items:flex-end; justify-content:center; opacity:0;
  transition:opacity .14s, transform .14s; }
.c4-fleche::before{ content:""; width:34%; aspect-ratio:1/.7; background:#FFC53D;
  clip-path:polygon(50% 100%, 0 0, 100% 0); filter:drop-shadow(0 0 6px rgba(255,197,61,.7)); }
.c4-fleche.libre{ opacity:.32; }
.c4-fleche.libre:hover, .c4-fleche.libre:focus-visible{ opacity:1; transform:translateY(3px); outline:none; }
.c4-fleche[disabled]{ opacity:0 !important; cursor:default; }
.c4-grille{ display:grid; grid-template-columns:repeat(7,1fr); gap:1.6%; }
.c4-case{ position:relative; aspect-ratio:1; border-radius:50%; cursor:pointer;
  background:radial-gradient(72% 72% at 50% 34%,#03060E,#080E1E 58%,#101A38);
  /* Un liseré clair sur le bord bas du trou : sans lui, les 42 trous se
     fondaient dans le bleu du plateau et la grille ne se lisait plus. */
  box-shadow:inset 0 4px 7px rgba(0,0,0,.95), inset 0 -2px 2px rgba(150,190,255,.22),
             0 1px 0 rgba(120,165,255,.18); }
.c4-case[data-plein]{ cursor:default; }
.c4-case.survol{ box-shadow:inset 0 3px 6px rgba(0,0,0,.9), 0 0 0 2px rgba(255,197,61,.45); }
/* Le jeton : un disque pose DANS le trou, qui tombe a l'arrivee. */
.c4-case .pion{ position:absolute; inset:5.5%; border-radius:50%;
  box-shadow:inset 0 -4px 7px rgba(0,0,0,.45), inset 0 4px 5px rgba(255,255,255,.34),
             0 2px 5px rgba(0,0,0,.55); }
.c4-case .pion.p1{ background:radial-gradient(68% 68% at 34% 26%,#FF9AA2,#FF4655 50%,#7E0F1A); }
.c4-case .pion.p2{ background:radial-gradient(68% 68% at 34% 26%,#9CC8FF,#2E7BFF 50%,#0D3374); }
.c4-case .pion.tombe{ animation:c4tombe .34s cubic-bezier(.4,1.5,.6,1); }
@keyframes c4tombe{ from{ transform:translateY(-560%) } to{ transform:translateY(0) } }
/* La ligne gagnante : ce sont les quatre cases exactes renvoyees par le
   serveur, pas une approximation calculee ici. */
.c4-case.gagne .pion{ animation:c4gagne .8s ease-in-out infinite alternate; z-index:2; }
@keyframes c4gagne{ to{ box-shadow:inset 0 -4px 7px rgba(0,0,0,.45), inset 0 4px 5px rgba(255,255,255,.34),
             0 0 0 3px rgba(255,197,61,.95), 0 0 22px rgba(255,197,61,.85); } }
.c4-plateau.fini .c4-case{ cursor:default; }
.c4-plateau.attend{ opacity:.75; }

/* ---- le mot de la fin ---- */
.c4-msg{ text-align:center; font-family:'Anton',sans-serif; letter-spacing:1px;
  font-size:clamp(17px,5vw,28px); margin:14px 0 0; min-height:1.15em; line-height:1.15; }
.c4-msg.g{ color:#7CFF9B; } .c4-msg.p{ color:#F2685E; } .c4-msg.n{ color:#E7C97A; }
.c4-sousmsg{ text-align:center; font-size:12.5px; color:var(--dim); margin-top:5px; line-height:1.5; }

/* ---- la revanche : on choisit une somme, elle n'est pas forcement l'ancienne ---- */
.c4-revanche{ max-width:420px; margin:14px auto 0; }
.c4-revanche .c4-lb{ justify-content:center; }

.c4-hist{ max-width:560px; margin:18px auto 0; font-size:12px; color:var(--dim); }
.c4-hist h4{ margin:0 0 4px; font-size:12px; letter-spacing:1.2px;
  text-transform:uppercase; color:#E7C97A; text-align:center; }
.c4-hist table{ width:100%; border-collapse:collapse; }
.c4-hist th, .c4-hist td{ padding:3px 6px; text-align:left; }
.c4-hist th{ color:#E7C97A; font-weight:800; font-size:11px; text-transform:uppercase; letter-spacing:.8px; }
.c4-hist td:last-child, .c4-hist th:last-child{ text-align:right; }
.c4-hist .g{ color:#7CFF9B; font-weight:800; }
.c4-hist .p{ color:#F2685E; font-weight:800; }

